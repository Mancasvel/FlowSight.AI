# Bundled legacy checkpoint (not used by current agent)

This directory holds an older merged multimodal checkpoint kept for reference. The FlowSight agent uses the GGUF stack under `local_llm/` configured by `setup_llm.py`.

See `LICENSE` in this directory for third-party terms.
